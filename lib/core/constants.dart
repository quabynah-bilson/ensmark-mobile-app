final class AppConstants {
  const AppConstants._();

  static const simulatedDuration = Duration(seconds: 2);

  static const String appName = 'Ensmark';
  static const String preferredDatePattern = 'dd/MM/yyyy';
}
