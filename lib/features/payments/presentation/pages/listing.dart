import 'package:flutter/material.dart';

class PaymentsListingScreen extends StatelessWidget {
  const PaymentsListingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Payments Listing')));
  }
}
